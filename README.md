# CSF303 C++ Practical — Quick Reference

## Q01 — Personal Introduction
Store your full name and student number as variables.
Use `cout` to print a neat profile card with labels like Name, Student No, and Course.
Everything is hardcoded — no user input needed.

![1](./assets/image/1.png)

## Q02 — Arithmetic with Student Number
Store your student number as an integer.
Calculate and display: the sum of its digits, whether it is odd or even, the remainder when divided by 7, and the number multiplied by 3.

![2](./assets/image/2.png)


## Q03 — String Manipulation & Analysis
Store your full name as a string.
Display the total character count, the name in uppercase and lowercase, your initials, and the length of your first name only.
All of this uses built-in string functions like `length()`, `toupper()`, and `tolower()`.

![3](./assets/image/3.png)


## Q04 — User Input & Type Conversion
Ask the user to type in their name, student number, and age.
Use the age to calculate what year they were born and what year they will turn 100.
Print everything out in a neat summary.

![4](./assets/image/4.png)


## Q05 — Conditional Grade Classification
Ask the user to enter a mark (0–100).
Use if-else to decide if it is a Distinction, Merit, Pass, or Fail.
If the mark is outside 0–100, show an error message instead.

![5](./assets/image/5.png)


## Q06 — Loop-Based Pattern Generation
Use a loop to print your first name multiple times (once per letter in your name).
Use a nested loop to draw a triangle made of asterisks.
Use another loop to print the times table for the last digit of your student number.

![6](./assets/image/6.png)


## Q07 — Array Operations & Statistics
Store 5 test marks in an array.
Find the highest, lowest, and average mark.
Count how many marks are above average.
Draw a simple bar made of asterisks for each mark.

![7](./assets/image/7.png)

## Q08 — Function Design & Modular Programming
Write four separate functions:
- `digitSum` — adds up all the digits of a number
- `isPrime` — checks if a number is prime
- `countVowels` — counts vowels in a string
- `reverseString` — reverses a string

Call each function and display the results.

![8](./assets/image/8.png)

## Q09 — Vector & Dynamic Collections
Ask the user to enter 6 numbers and store them in a vector.
Sort the vector, then find the min, max, sum, and average.
Remove all numbers that are below the average and display what is left.

![9](./assets/image/9.png)


## Q10 — Classes & Object-Oriented Design
Create a `Student` class that stores a name, student number, and a list of marks.
All data is private — it can only be accessed through methods.
Add marks using `addMark()`, then call `printReport()` to display the full academic summary.


![10](./assets/image/10.png)
